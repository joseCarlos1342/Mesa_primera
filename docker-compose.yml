version: "3.8"

services:
  redis:
    image: redis:7-alpine
    container_name: mesa_primera_redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

volumes:
  redis_data:
