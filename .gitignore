# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# ── Dependencies ──
node_modules
.pnpm-store
.pnp
.pnp.js

# ── Local env files ──
.env
.env.*.local

# ── Build outputs ──
.next/
out/
build
dist

# ── Package manager logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── OS / IDE ──
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/
*.sublime-*
.cursorrules

# ── AI Agents / local configs ──
.agents/
.agent/
_agents/
_agent/
.gemini/
.claude/
.opencode/
.github/agents/
.github/instructions/

# ── Turbo / Vercel ──
.turbo
.vercel
.vercel-env-*
vercel-env-*
.env.vercel

# ── Supabase local state ──
supabase/.temp/
supabase/.branches/

# ── Redis dumps ──
dump.rdb

# ── Testing / coverage ──
coverage
test-results/
.playwright/
.playwright-cli/
.unlighthouse/

# ── Playwright / E2E snapshots (local dev) ──
/replays/
apps/game-server/replays/
e2e/snapshots/*.yaml

# ── PWA generated files ──
sw.js
sw.js.map
workbox-*.js
workbox-*.js.map

# ── Credentials & secrets ──
*.key
*.secret
.ssh_mesa/
*.env.backup
*.env.bak
*credentials*.json
*service-account*.json
*.p12
*.pfx

# ── Temporary / debug files ──
diff.tmp
*-debug.*
tatus
t\ --json\ number,title,headRefName,state\ --limit\ 10

# ── Local debug & test scripts ──
# Nota: los scripts oficiales de test deben vivir en packages/*/test/ o __tests__/
test-*.js
test-*.ts
test_*.js
test_*.ts
native-test.ts
simulate-game.ts
google_oauth_test.mjs
inspect_login.js
test_login.js
test-update.js

# ── Dev / misc files no trackeados ──
dev.sh
skills-lock.json
skills*.json
*plan.md

# ── Internal documentation (business logic, sprints, deployment) ──
docs/
plan_primera.md
CHANGELOG.md
